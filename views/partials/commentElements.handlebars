<div class="card">
  <div class="card-body">
    <h5 class="card-title">{{text_body}}</h5>
    <p class="card-text text-muted">{{format_date created_at}}</p>
    <a href="/post/{{post_id}}" class="btn btn-primary">View Post</a>
  </div>
</div>